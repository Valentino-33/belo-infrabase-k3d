# Settings usados por: make tekton-apply (helm template --set tekton.enabled=true)
# Todo lo demás (strategy, image.repository, replicas, etc.) va en <env>/values.yaml
tekton:
  dockerhubUser: valentinobruno
  gitopsRepoUrl: https://github.com/Valentino-33/belo-infrabase-k3d
