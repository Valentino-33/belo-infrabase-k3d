# Token de GitHub con permisos de escritura al repo (para bump-gitops push).
# Reemplazar <GITHUB_TOKEN> con un Personal Access Token (scope: repo).
apiVersion: v1
kind: Secret
metadata:
  name: github-token
  namespace: tekton-pipelines
type: Opaque
stringData:
  token: <GITHUB_TOKEN>
